# Anchored to the repo root ON PURPOSE. A bare `netbox-mcp` also matches
# charts/netbox-mcp/ and cmd/netbox-mcp/, which is how a whole chart and a
# main package once went missing while every check stayed green.
/netbox-mcp
/dist/
